# this was necessary to install

sudo pip3 install scikit-learn --index-url https://piwheels.org/simple --force

# and to output a pdf of what's happening:

sudo apt install python3-pydotplus graphviz


